# Pipeline reprodutível (executa treino e avaliação a partir dos dados versionados).
# Os artefatos de modelo são versionados com `dvc add` (ver models/*.dvc).
# Rodar com:  dvc repro
stages:
  train:
    cmd: >
      uv run python -m recommender.interfaces.cli.train
      --model-type baseline --model-name baseline-v1
      --data-dir data/raw --model-dir models
    deps:
      - data/raw/ratings.csv
      - data/raw/movies.csv
      - src/recommender

  evaluate:
    cmd: >
      uv run python -m recommender.interfaces.cli.evaluate
      --model-name baseline-v1 --data-dir data/raw --model-dir models
    deps:
      - data/raw/ratings.csv
      - src/recommender
